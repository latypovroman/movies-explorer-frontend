import React from 'react';
import SearchForm from "../SearchForm/SearchForm";

const Movies = () => {
    return (
        <main>
            <SearchForm />
        </main>
    );
};

export default Movies;